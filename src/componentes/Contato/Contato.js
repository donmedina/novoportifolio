import styles from './Contato.module.sass'

export const Contato = () => {
    return (
        <section className={styles.contato}>
            <div>
                <h2>Contato</h2>
            </div>
        </section>
    )
}
